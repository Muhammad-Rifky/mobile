class Item {
  final String name;
  final int price;
  final String imagePath; // Changed this to imagePath for clarity
  final int stock;
  final double rating;

  Item(this.name, this.price, this.imagePath, this.stock, this.rating);
}